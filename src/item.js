export default class Item {
  constructor({id, name, description, char, fg, bg}) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.char = char;
    this.fg = fg;
    this.bg = bg;
  }
}
